<%- include('../layout/header', { title: 'Batch Detail' }) %><main class="mx-auto max-w-5xl px-4 py-10"><h1 class="font-display text-3xl font-bold"><%= batch.name %></h1><div class="mt-6 grid gap-2"><% batch.tags.forEach(t=>{ %><div class="rounded bg-white p-3 font-mono"><%= t.tag_id %> · <%= t.is_active ? 'Active' : 'Inactive' %></div><% }) %></div></main><%- include('../layout/footer') %>
